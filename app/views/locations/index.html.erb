<h1>Locations</h1>

<% @locations.each do |l| %>
  <div>
    <h2><%= link_to l.name.titleize, location_events_path(l) %></h2>
    <ul>
      <li><%= l.address %></li>
      <li><%= l.city.titleize %>, <%= l.state.titleize %></li>
    </ul>
  </div>
<% end %>
